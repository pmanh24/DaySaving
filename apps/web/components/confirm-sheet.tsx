"use client";
import { LoaderCircle, Sparkles, X } from "lucide-react";
import { money } from "@/lib/format";
export function ConfirmSheet({ number, amount, total, onClose, onConfirm, loading }: { number:number; amount:number; total:number; onClose:()=>void; onConfirm:()=>void; loading:boolean }) { return <div className="sheet-backdrop" role="presentation" onMouseDown={(event)=>{if(event.target===event.currentTarget&&!loading)onClose();}}><section className="sheet" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div className="drag"/><div className="hero-row"><div><p className="eyebrow" style={{color:"#6c63d9"}}>TIẾT KIỆM HÔM NAY</p><h2 id="confirm-title">Xác nhận khoản tiết kiệm</h2></div><button className="icon-button" style={{color:"#121237",borderColor:"#dddde8",background:"#fff"}} onClick={onClose} disabled={loading} aria-label="Đóng"><X size={18}/></button></div><div className="sheet-amount">{money(amount)}</div><p>Bạn sẽ đánh dấu ô số <strong>{number}</strong> cho ngày hôm nay. Backend sẽ tự tính số tiền theo giá trị của ô.</p><div className="sheet-summary"><div className="sheet-stat"><span>Tổng hiện tại</span><strong>{money(total)}</strong></div><div className="sheet-stat"><span>Sau khi xác nhận</span><strong>{money(total+amount)}</strong></div></div><div className="sheet-actions"><button className="button secondary" onClick={onClose} disabled={loading}>Hủy</button><button className="button primary" onClick={onConfirm} disabled={loading}>{loading?<><LoaderCircle size={17} className="spin"/> Đang lưu</>:<><Sparkles size={17}/> Xác nhận</>}</button></div></section></div>; }
