"use client";
import { Check, CircleHelp } from "lucide-react";
import type { BoardCell } from "@saving/shared";
import { boardMoney, money } from "@/lib/format";
export function SavingBoard({ cells, suggestedNumber, onSelect }: { cells: BoardCell[]; suggestedNumber:number|null; onSelect:(cell:BoardCell)=>void }) { return <div className="board-card"><div className="section-row" style={{marginBottom:12}}><div><h2 className="section-title">Bảng tiết kiệm</h2><p className="section-caption">Chạm vào một ô để xem chi tiết</p></div><CircleHelp size={18} color="#9a9aae"/></div><div className="board" aria-label="Bảng 100 ô tiết kiệm">{cells.map((cell)=><button key={cell.number} className={`cell ${cell.status==="COMPLETED"?"done":""} ${suggestedNumber===cell.number?"suggested":""}`} disabled={cell.status==="COMPLETED"} onClick={()=>onSelect(cell)} aria-label={`Ô số ${cell.number}, ${money(cell.amount)}${cell.status==="COMPLETED"?", đã hoàn thành":""}`}><small>{cell.number}</small>{cell.status==="COMPLETED"?<Check size={16}/>:boardMoney(cell.amount)}</button>)}</div><div className="board-legend"><span className="legend-item"><i className="legend-dot done"/>Đã xong</span><span className="legend-item"><i className="legend-dot"/>Có thể chọn</span><span className="legend-item"><i className="legend-dot suggested"/>Gợi ý</span></div></div>; }
